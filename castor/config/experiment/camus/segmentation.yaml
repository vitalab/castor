# @package _global_

defaults:
  - override /task: segmentation
  - override /data: camus

trainer:
  max_epochs: 200
  precision: 16
  benchmark: True
  accumulate_grad_batches: 4

data:
  batch_size: 32
